# OS
.DS_Store

# Node
node_modules/
package-lock.json

# Local scripts and build tools (not for public repo)
*.mjs
*.js
output/
tmp/

# Sensitive
*.csv
*.env
*.enc
*.json
*.pem

# Local organizer folder — never publish (clinician tools, keys, workflow notes,
# printable copies, and an offline duplicate of the patient forms).
# The live patient forms are the copies at the repo root.
00_START_HERE/
